{% extends 'base.html.twig' %}

{% block title %}Mot de passe oublié{% endblock %}

{% block body %}

<div class="main lightgreen">
    <div class="container-fluid">
        <div class="search row">
            <div class="col-12">
                <h1 class="white__title">Mot de passe oublié</h1>
            </div>
        </div>
        <div class="row">
            <div class="col-12">
                <div class="signin__explaination">
                    <p>Merci de renseigner l'adresse email saisie lors de l'inscription.</p>
                    <p>On va t'envoyer un lien pour ré-initialiser ton mot de passe!</p>
                </div>
            </div>
        </div>
        <div class="row justify-content-center">
            <div class="col-12 col-md-8 col-lg-6 col-xl-4">
                {{ form_start(emailForm)}}
                    {{ form_row(emailForm.email) }}
                    <div class="text-center">
                        <button type="submit" class="btn btn-orange">Envoyer</button>
                    </div>
                {{ form_end(emailForm) }}
            </div>
        </div>
    </div>
</div>

{% endblock %}